name: Build Base Images

on:
    push:
        branches:
            - develop
            - main
        paths:
            - 'Dockerfile.base'
    workflow_dispatch:

env:
    IMAGE_NAME: ghcr.io/proycer/php-base
    IMAGE_TAG: 8.4-fpm-alpine

jobs:
    build-base:
        name: Build Base Image (Runtime)
        runs-on: ubuntu-latest
        permissions:
            contents: read
            packages: write
        steps:
            -   name: Checkout repository
                uses: actions/checkout@v4

            -   name: Log in to GitHub Container Registry
                uses: docker/login-action@v3
                with:
                    registry: ghcr.io
                    username: ${{ github.actor }}
                    password: ${{ secrets.GITHUB_TOKEN }}

            -   name: Set up Docker Buildx
                uses: docker/setup-buildx-action@v3

            -   name: Build and Push Base Image
                uses: docker/build-push-action@v6
                with:
                    context: .
                    file: Dockerfile.base
                    target: base
                    push: true
                    tags: ${{ env.IMAGE_NAME }}:${{ env.IMAGE_TAG }}
                    cache-from: type=gha
                    cache-to: type=gha,mode=max
                    provenance: false
                    sbom: false

    build-ci:
        name: Build Dev Image (Runtime + Composer)
        runs-on: ubuntu-latest
        needs: build-base
        permissions:
            contents: read
            packages: write
        steps:
            -   name: Checkout repository
                uses: actions/checkout@v4

            -   name: Log in to GitHub Container Registry
                uses: docker/login-action@v3
                with:
                    registry: ghcr.io
                    username: ${{ github.actor }}
                    password: ${{ secrets.GITHUB_TOKEN }}

            -   name: Set up Docker Buildx
                uses: docker/setup-buildx-action@v3

            -   name: Build and Push CI Image
                uses: docker/build-push-action@v6
                with:
                    context: .
                    file: Dockerfile.base
                    target: ci
                    push: true
                    tags: ${{ env.IMAGE_NAME }}-dev:${{ env.IMAGE_TAG }}
                    cache-from: type=gha
                    cache-to: type=gha,mode=max
                    provenance: false
                    sbom: false
